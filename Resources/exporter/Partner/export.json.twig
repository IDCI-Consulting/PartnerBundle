{
    "id": "{{ entity.id }}",
    "name": "{{ entity.name }}",
    "phone": "{{ entity.phone }}",
    "mail": "{{ entity.mail }}",
    "sociallinks":
    [
    {% for sociallink in entity.sociallinks %}
        {% if loop.first %}
        { "id": "{{ sociallink.id }}", "uri": "{{ sociallink.uri }}" }
        {% else %}
        ,{ "id": "{{ sociallink.id }}", "uri": "{{ sociallink.uri }}" }
        {% endif %}
    {% endfor %}
    ],
    "offers":
    [
    {% for offer in entity.offers %}
        {% if loop.first %}
        { "id": "{{ offer.id }}", "name": "{{ offer.name }}" }
        {% else %}
        ,{ "id": "{{ offer.id }}", "name": "{{ offer.name }}" }
        {% endif %}
    {% endfor %}
    ],
    "locations":
    [
    {% for location in entity.locations %}
        {% if loop.first %}
        {
        "id": "{{ location.id }}",
        "name": "{{ location.name }}",
        "address": "{{ location.address }}",
        "zip_code": "{{ location.zipCode}}",
        "city": "{{ location.city }}",
        "country": "{{ location.country }}",
        }
        {% else %}
        ,{
        "id": "{{ location.id }}",
        "name": "{{ location.name }}",
        "address": "{{ location.address }}",
        "zip_code": "{{ location.zipCode}}",
        "city": "{{ location.city }}",
        "country": "{{ location.country }}",
        }
        {% endif %}
    {% endfor %}
    ]
}