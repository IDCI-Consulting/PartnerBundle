<category id="{{ entity.id }}" color="{{ entity.color }}" level="{{ entity.level }}">
  <name>{{ entity.name }}</name>
  <description><![CDATA[{{ entity.description }}]]></description>
  <tree>{{ entity.tree }}</tree>
</category>