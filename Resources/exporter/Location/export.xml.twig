<location id="{{ entity.id }}">
    <name>{{ entity.name }}</name>
    <address>{{ entity.address }}</address>
    <zip_code>{{ entity.zipCode }}</zip_code>
    <city>{{ entity.city }}</city>
    <country>{{ entity.country }}</country>
</location>